//This class does search in buget history txt. file and outputs the results on a screen.
// It change nothing, only reads the values.
// Changes test.

import java.util.Scanner;

public class Search {

    Scanner scanner = new Scanner(System.in);

    public Scanner getScanner() {
        scanner.next();

        return scanner;
    }


}
